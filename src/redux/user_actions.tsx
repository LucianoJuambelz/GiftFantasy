import axios from "axios";
import { Bookmark } from "../Models/user.models";

export const USER_POST = "USER_POST";

// export const getGifs = (bookmark: Bookmark) => async (dispatch: Function) => {
//   axios.post();
//   dispatch({ type: USER_POST });
// };
